import { SButton, SContainer } from "../styles/ButtonsUtils.style";

export default function ButtonsUtils() {
  return(
    <SContainer>
      <SButton>Editar</SButton>
      <SButton>Checar</SButton>
    </SContainer>
  )
}
